
ADD THESE COLUMNS TO SUPABASE:

alter table orders add column credentials_email text;
alter table orders add column credentials_password text;

---

AUTO-REVEAL RULE:
- If status === 'PAID' → show credentials
- If status === 'LOCKED' → hide credentials
