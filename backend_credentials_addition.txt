CREDENTIAL AUTO-REVEAL RULE:
- If status === 'PAID' → show credentials
- If status === 'LOCKED' → hide credentials and show lock_reason

The schema now includes credentials_email, credentials_password, and credentials_id.
